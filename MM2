-- rithan is trash he needs to cheat to win
-- stop camlocking lil kid

local players = game:GetService("Players")
local localPlayer = players.LocalPlayer
local playerGui = localPlayer:WaitForChild("PlayerGui")

-- Create the GUI elements completely through script
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "KeySystem"
screenGui.Parent = playerGui

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 300, 0, 150)
frame.Position = UDim2.new(0.5, -150, 0.5, -75)
frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
frame.Parent = screenGui

local label = Instance.new("TextLabel")
label.Size = UDim2.new(1, 0, 0, 40)
label.Text = "Enter Key:"
label.TextColor3 = Color3.fromRGB(255, 255, 255)
label.BackgroundTransparency = 1
label.Parent = frame

local textBox = Instance.new("TextBox")
textBox.Size = UDim2.new(0, 200, 0, 30)
textBox.Position = UDim2.new(0.5, -100, 0.4, 0)
textBox.PlaceholderText = "Key here..."
textBox.Parent = frame

local button = Instance.new("TextButton")
button.Size = UDim2.new(0, 100, 0, 30)
button.Position = UDim2.new(0.5, -50, 0.7, 0)
button.Text = "Submit"
button.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.Parent = frame

local correctKey = "kevmm2"
local keyVerified = false
local startTime = os.clock() -- Captures the exact start time

-- Combined loop for high-speed printing and strict timer tracking
task.spawn(function()
	while true do
		local elapsed = os.clock() - startTime
		
		-- Strict kick threshold check (10 seconds)
		if elapsed >= 10 then
			localPlayer:Kick("stop cheating kid")
			break 
		end
		
		-- High-speed print execution when key matches
		if keyVerified then
			-- Combined lines into one string via \n to maximize speed safely
			print("Stop cheating nigger\nrishi is so trash at the game\nram likes dick up his ass")
		end
		
		-- Controlled micro-delay keeps spam blazing fast without crashing the client
		task.wait(0.02) 
	end
end)

-- Button click handler
button.MouseButton1Click:Connect(function()
	if textBox.Text == correctKey then
		keyVerified = true
		label.Text = "check the consle"
		label.TextColor3 = Color3.fromRGB(0, 255, 0)
		textBox.Visible = false
		button.Visible = false
	else
		label.Text = "Incorrect Key!"
		label.TextColor3 = Color3.fromRGB(255, 0, 0)
	end
end)
